apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spider-lightedge.fullname" . }}-test-connection"
  labels:
    {{- include "spider-lightedge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spider-lightedge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
